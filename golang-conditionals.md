# GOLANG CONDITIONALS

## Compound OR If Statement
```golang
if req == nil || fname == nil {}
```

## Compound AND If Statement
```golang
if req != nil && fname == nil {
  fname = req.File.Name
}
```
